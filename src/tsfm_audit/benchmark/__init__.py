"""Fresh benchmark: evaluation data published after every audited checkpoint.

Uncontaminated by construction — not because a model card says so, but because
the observations did not exist when the models were trained.
"""
