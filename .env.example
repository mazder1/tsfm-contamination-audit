# Copy to .env and fill in. .env is gitignored - never commit a real token.
#
# ENTSO-E Transparency Platform, for the electricity source of the fresh
# benchmark. Free, but access is granted manually - see README.
ENTSOE_API_TOKEN=
